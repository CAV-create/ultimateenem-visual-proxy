openapi: 3.1.0
info:
  title: ultimateENEM Visual Proxy
  version: 1.2.2
  description: >
    Proxy do ultimateENEM para localizar PDFs no Dropbox, renderizar paginas,
    recortar recursos visuais e criar pranchas HTML de auditoria visual. O GPT
    deve autenticar com Chave API no header Authorization; nao use OAuth no GPT Builder.
servers:
  - url: https://ultimateenem-visual-proxy.onrender.com
security:
  - ApiKeyAuth: []
paths:
  /v1/dropbox/test-refresh:
    post:
      operationId: testar_oauth_dropbox_proxy
      summary: Testa se o proxy consegue renovar o token do Dropbox.
      description: >
        Testa a renovacao do token Dropbox pelo proxy. Use antes de buscar PDFs.
        Se falhar, revise as variaveis do Render.
      responses:
        "200":
          description: Token Dropbox renovado com sucesso pelo proxy.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  ok:
                    type: boolean
                  message:
                    type: string
                  dropbox_refresh_configured:
                    type: boolean
  /v1/dropbox/search:
    post:
      operationId: buscar_pdf_dropbox
      summary: Busca PDFs ou arquivos no Dropbox do projeto.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [query]
              properties:
                query:
                  type: string
                  description: Nome parcial do PDF ou trecho do caminho.
                path:
                  type: string
                  default: /ENEM 2026 App
                  description: Pasta inicial do Dropbox.
                max_results:
                  type: integer
                  default: 10
                  minimum: 1
                  maximum: 50
      responses:
        "200":
          description: Resultados encontrados no Dropbox.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  matches:
                    type: array
                    items:
                      type: object
                      additionalProperties: false
                      properties:
                        name:
                          type: string
                        path_display:
                          type: string
                        id:
                          type: string
                        client_modified:
                          type: string
  /v1/pdf/render-page:
    post:
      operationId: renderizar_pagina_pdf
      summary: Renderiza uma pagina inteira de um PDF do Dropbox e salva no Dropbox.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [pdf_path, page_number]
              properties:
                pdf_path:
                  type: string
                  description: Caminho completo do PDF no Dropbox.
                page_number:
                  type: integer
                  minimum: 1
                  description: Numero da pagina do PDF, iniciando em 1.
                dpi:
                  type: integer
                  default: 220
                  minimum: 96
                  maximum: 360
                output_folder:
                  type: string
                  description: Pasta Dropbox de saida.
                output_name:
                  type: string
                  description: Nome base do arquivo sem extensao.
                image_format:
                  type: string
                  enum: [png, webp]
                  default: png
                webp_quality:
                  type: integer
                  default: 88
                  minimum: 40
                  maximum: 100
      responses:
        "200":
          description: Pagina renderizada e salva.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VisualOperationResponse"
  /v1/pdf/crop:
    post:
      operationId: recortar_recurso_visual_pdf
      summary: Recorta uma regiao de uma pagina de PDF do Dropbox e salva PNG/WebP no Dropbox.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [pdf_path, page_number, bbox, output_basename]
              properties:
                pdf_path:
                  type: string
                  description: Caminho completo do PDF no Dropbox.
                page_number:
                  type: integer
                  minimum: 1
                  description: Numero da pagina do PDF, iniciando em 1.
                bbox:
                  type: object
                  additionalProperties: false
                  required: [x, y, w, h]
                  properties:
                    x:
                      type: number
                    y:
                      type: number
                    w:
                      type: number
                    h:
                      type: number
                units:
                  type: string
                  enum: [normalized, pixels]
                  default: normalized
                  description: normalized usa 0 a 1; pixels usa a pagina renderizada.
                dpi:
                  type: integer
                  default: 220
                  minimum: 96
                  maximum: 360
                pad_px:
                  type: integer
                  default: 10
                  minimum: 0
                  maximum: 120
                  description: Sangria ao redor do recurso.
                output_folder:
                  type: string
                  description: Pasta Dropbox de saida.
                output_basename:
                  type: string
                  description: Nome base dos arquivos gerados sem extensao.
                make_png:
                  type: boolean
                  default: true
                make_webp:
                  type: boolean
                  default: true
                webp_quality:
                  type: integer
                  default: 88
                  minimum: 40
                  maximum: 100
      responses:
        "200":
          description: Recurso visual recortado e salvo.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VisualOperationResponse"
  /v1/audit/create-board:
    post:
      operationId: criar_prancha_auditoria_visual
      summary: Cria prancha HTML de auditoria visual.
      description: >
        Cria HTML no Dropbox com original a esquerda, tratado a direita,
        checkbox, campo de motivo e botao de copiar parecer. Aceita aliases pt/en.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
              properties:
                audit_title:
                  type: string
                  default: AUDITORIA VISUAL
                  description: Titulo da prancha. Tambem aceita title ou titulo.
                title:
                  type: string
                  description: Alias aceito para audit_title.
                titulo:
                  type: string
                  description: Alias aceito para audit_title.
                output_folder:
                  type: string
                  description: Pasta Dropbox de saida. Tambem aceita folder ou pasta.
                output_name:
                  type: string
                  description: Nome base do HTML. Tambem aceita name, nome ou nome_arquivo.
                cases:
                  type: array
                  minItems: 1
                  maxItems: 80
                  items:
                    $ref: "#/components/schemas/AuditCaseInput"
                casos:
                  type: array
                  minItems: 1
                  maxItems: 80
                  items:
                    $ref: "#/components/schemas/AuditCaseInput"
                items:
                  type: array
                  minItems: 1
                  maxItems: 80
                  items:
                    $ref: "#/components/schemas/AuditCaseInput"
                questoes:
                  type: array
                  minItems: 1
                  maxItems: 80
                  items:
                    $ref: "#/components/schemas/AuditCaseInput"
      responses:
        "200":
          description: Prancha HTML criada e salva no Dropbox.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditBoardResponse"
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: Authorization
      description: Use Bearer seguido da VISUAL_PROXY_API_KEY. Nao use OAuth.
  schemas:
    AuditCaseInput:
      type: object
      additionalProperties: true
      properties:
        case_id:
          type: string
          description: Identificador curto, como Q07. Tambem aceita id, questao, q ou numero.
        id:
          type: string
          description: Alias aceito para case_id.
        questao:
          type: string
          description: Alias aceito para case_id.
        title:
          type: string
          description: Titulo curto. Tambem aceita titulo, descricao ou description.
        titulo:
          type: string
          description: Alias aceito para title.
        descricao:
          type: string
          description: Alias aceito para title.
        original_path:
          type: string
          description: Caminho Dropbox do original. Tambem aceita original e raw_path.
        original:
          type: string
          description: Alias aceito para original_path.
        original_dropbox_path:
          type: string
          description: Alias aceito para original_path.
        treated_path:
          type: string
          description: Caminho Dropbox da tratada. Tambem aceita treated e tratada_path.
        treated:
          type: string
          description: Alias aceito para treated_path.
        tratada_path:
          type: string
          description: Alias aceito para treated_path.
        notes:
          type: string
          description: Observacao opcional. Tambem aceita observacoes.
        observacoes:
          type: string
          description: Alias aceito para notes.
    UploadedAsset:
      type: object
      additionalProperties: false
      properties:
        format:
          type: string
          enum: [png, webp]
        dropbox_path:
          type: string
        temporary_link:
          type: string
        bytes:
          type: integer
        width:
          type: integer
        height:
          type: integer
    VisualOperationResponse:
      type: object
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [ok]
        pdf_path:
          type: string
        page_number:
          type: integer
        dpi:
          type: integer
        page_width:
          type: integer
        page_height:
          type: integer
        crop_box_pixels:
          type: array
          description: Vazio em pagina inteira; no recorte vem [x1, y1, x2, y2].
          items:
            type: integer
        assets:
          type: array
          items:
            $ref: "#/components/schemas/UploadedAsset"
    AuditBoardResponse:
      type: object
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [ok]
        audit_title:
          type: string
        case_count:
          type: integer
        dropbox_path:
          type: string
        temporary_link:
          type: string
